## Create interactive dashboard

### The Prompt:
```text
* Create following dataframe: It has these columns: Name, City, Country, Color, Build, Data Surface A, Data Surface B, Data Surface C, Data Surface D 

* Generate 100 random data for the dataframe

* Create an interactive dashboard using plotly (dash | panel + hvplot) for the following:
1. a grid of bar charts for each of the data columns, grouped by Country and Color,  
2. the bar chart should first sorted by overall average of Country group, then secondly, sorted by Color group
```